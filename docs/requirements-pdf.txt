# Requirements for building the offline user-guide PDF (Installer\build-user-guide.ps1).
# Kept separate from requirements.txt so the online docs build stays minimal.
# The PDF is rendered with headless Microsoft Edge / Chrome, so there are no native deps here.
-r requirements.txt
mkdocs-print-site-plugin
